{
  "name": "transcriber",
  "version": "0.0.0",
  "type": "module",
  "prettier": "@cloud-cli/prettier-config",
  "scripts": {
    "ci": "npm i && npm run build",
    "build": "vite build"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloud-cli/prettier-config": "^1.0.0",
    "vite": "^5.2.0"
  }
}
